---
import '@styles/global.css';
import { Font } from 'astro:assets';
import { ClientRouter } from 'astro:transitions';
import SearchModal from '@components/ui/SearchModal.astro';

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
// const isProd = !import.meta.env.DEV;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <title>{title}</title>

    <Font cssVariable="--font-host-grotesk" preload />
    <Font cssVariable="--font-inter" preload />

    <!-- {isProd && (
      <>
        <link href="/pagefind/pagefind-component-ui.css" rel="stylesheet" />
        <script src="/pagefind/pagefind-component-ui.js" type="module"></script>
      </>
    )} -->

    <meta name="description" content={description} />

    <ClientRouter />

    <!-- <script src="../scripts/config/lenis.ts"></script> -->
  </head>
  <body class="bg-neutral-950 text-base font-light text-pretty text-neutral-50">
    <slot />
    <!-- {isProd && <SearchModal />} -->
    <SearchModal />
  </body>
</html>
