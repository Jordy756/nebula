---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'th'> {}

const { align, class: className, ...rest } = Astro.props;
---

<th class:list={['px-4 py-3 font-semibold whitespace-nowrap', className]} scope="col" align={align} {...rest}>
  <slot />
</th>
