---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'td'> {}

const { align, class: className, ...rest } = Astro.props;
---

<td class:list={['px-4 py-3', className]} align={align} {...rest}>
  <slot />
</td>
