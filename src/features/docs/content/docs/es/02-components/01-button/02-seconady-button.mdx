---
title: Botones Secundarios
description: Descubre cómo integrar componentes interactivos directamente en tus archivos Markdown.
---

import Note from '@components/mdx/Note.astro';

## ¿Qué es Nebula?

Nebula no es una integración cerrada y oculta. Es tu propio laboratorio de componentes para documentación. Eres libre de modificar el código fuente de los componentes, los layouts y el diseño completo.

### Características

- **HTML semántico**: Usa `<nav>`, `<aside>`, `<article>`, `<header>`.
- **Propiedad total**: El código vive en tu carpeta `src/`.
- **Tailwind CSS v4**: Estilizado moderno y sin archivos gigantes.
- **Cero "div soup"**: Solo las etiquetas que necesitas.

### Siguiente paso

Ve a la sección de [Instalación](/docs/instalacion) para aprender cómo configurar tu proyecto.

## Bash

```bash
npm install nebula-docs
```

## Codigo TypeScript

```ts
import { getCollection } from 'astro:content';
const docsEntries = await getCollection('docs');
```

---

## Table

| Característica  | Descripción                                                     |
| --------------- | --------------------------------------------------------------- |
| HTML semántico  | Usa etiquetas como `<nav>`, `<aside>`, `<article>`, `<header>`. |
| Propiedad total | El código vive en tu carpeta `src/`.                            |
| Tailwind CSS v4 | Estilizado moderno y sin archivos gigantes.                     |
| Cero "div soup" | Solo las etiquetas que necesitas.                               |

<Note type="info" title="Nota de información">
  <p>Esta es una nota de información.</p>
</Note>

<Note type="warning" title="Nota de advertencia">
  <p>Esta es una nota de advertencia.</p>
</Note>

<Note type="error" title="Nota de error">
  <p>Esta es una nota de error.</p>
</Note>
