---
import GithubIcon from '@components/icons/GithubIcon.astro';
import LanguagePicker from '@components/ui/LanguagePicker.astro';
import Logo from '@components/ui/Logo.astro';
import SearchTrigger from '@components/ui/SearchTrigger.astro';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<header
  data-pagefind-ignore
  class:list={[
    'sticky top-0 z-50 flex items-center justify-between gap-4 border-b border-neutral-800 bg-neutral-950 px-4 py-2',
    className,
  ]}
>
  <a href={`/${Astro.currentLocale ?? 'es'}/`}>
    <Logo width={131.25} height={32} />
  </a>
  <SearchTrigger />
  <LanguagePicker />
  <div class="flex items-center gap-2">
    <a href="https://github.com/withastro/astro" target="_blank">
      <GithubIcon width={24} height={24} class="size-6 text-neutral-50" />
    </a>
    <a href="https://twitter.com/withastro" target="_blank">
      <GithubIcon width={24} height={24} class="size-6 text-neutral-50" />
    </a>
  </div>
</header>
